# Project1_Taskmanager
A lightweight Python CLI application for managing tasks from the terminal. Tasks are saved persistently in a JSON file with no external dependencies. Users can add, list, complete, and delete tasks with priority levels. Includes 9 unit tests using Python's built-in unittest module..
